<!DOCTYPE html>
<html>
  <head>
    <title>Welcome to CHITTER!</title>
    <style>
      /* Add some basic styling to the page */
      body {
        background-color: #f5f5f5;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }

      /* Center the content of the page */
      .container {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        flex-direction: column;
      }

      /* Style the heading */
      h1 {
        font-size: 3rem;
        text-align: center;
        color: #333;
        margin: 0;
      }

      /* Style the button */
      .button {
        padding: 1rem 2rem;
        font-size: 1.2rem;
        background-color: #0066cc;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        margin-top: 2rem;
      }

      /* Style the button when hovered */
      .button:hover {
        background-color: #0052a3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Welcome to CHITTER!</h1>
      <form action="/peeps">
        <button class="button">Enter the Peep Zone</button>
      </form>
    </div>
  </body>
</html>